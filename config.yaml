crml:
  log_level: INFO
  data_dir: data
  models_dir: models
  logs_dir: logs

mqtt:
  host: host.docker.internal
  port: 1883
  client_id: crml
  topics:
    subscribe: "robots/#"
    publish: "crml/#"

api:
  host: 0.0.0.0
  port: 8100
